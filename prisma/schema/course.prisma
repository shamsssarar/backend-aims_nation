// --- COURSE MODELS ---

model Course {
  id          String    @id @default(uuid())
  title       String
  description String?
  courseFee Int
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  deletedAt   DateTime?
}
