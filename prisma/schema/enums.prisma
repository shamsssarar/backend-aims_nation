enum Role {
  ADMIN
  TEACHER
  STUDENT
}

enum EnrollmentStatus {
  ACTIVE
  DROPPED
  COMPLETED
}

enum PaymentStatus {
  PENDING
  PAID
  OVERDUE
  CANCELLED
}

enum BehaviorStatus {
  EXCEPTIONAL
  NORMAL
  NEEDS_ATTENTION
}

enum ApplicationStatus {
  PENDING
  REVIEWED
  HIRED
  REJECTED
}